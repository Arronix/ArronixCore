{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Arronix/ArronixCore/blob/main/src/Arronix.Plugins/Manifest/plugin.schema.json",
  "title": "Arronix extension manifest",
  "description": "EDITOR AID ONLY. The runtime gate is Arronix.Plugins.Manifest.PluginManifestReader plus PluginManifestValidator, which read into required members with unmapped members disallowed. This document exists so an editor can offer completion; it is never consulted at load time and must not be treated as authoritative if the two ever disagree.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "name", "version", "contracts", "entryAssembly", "capabilities"],
  "properties": {
    "schemaVersion": {
      "description": "The version of this manifest format. Only 0 is understood today.",
      "type": "integer",
      "const": 0
    },
    "id": {
      "description": "Lower-case alphanumeric segments separated by dots, starting with a letter.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(\\.[a-z0-9]+)*$"
    },
    "name": {
      "description": "The name shown to an operator.",
      "type": "string",
      "minLength": 1
    },
    "version": {
      "description": "The extension's own version, in Semantic Versioning 2.0.0 form.",
      "type": "string",
      "pattern": "^[0-9]+(\\.[0-9]+){0,2}(-[0-9A-Za-z.-]+)?(\\+[0-9A-Za-z.-]+)?$"
    },
    "description": {
      "description": "An optional sentence describing what the extension is for.",
      "type": "string"
    },
    "contracts": {
      "description": "The contract versions the extension declares it works against.",
      "type": "object",
      "additionalProperties": false,
      "required": ["arronix"],
      "properties": {
        "arronix": {
          "description": "The compatible plugin-consumable Arronix.Abstractions SDK range, in a documented subset of npm/yarn syntax: comparison operators, whitespace conjunction, '||' alternation and partial versions. Caret, tilde, wildcards and hyphen ranges are rejected.",
          "type": "string",
          "minLength": 1
        }
      }
    },
    "entryAssembly": {
      "description": "The bare file name of the assembly holding the single IPluginModule. Resolved inside the extension's own folder; separators and parent-directory segments are rejected.",
      "type": "string",
      "pattern": "^[^\\\\/:]+\\.(dll|DLL)$"
    },
    "capabilities": {
      "description": "The privileges the extension declares. Enforcement is bidirectional: a declared capability must have a matching registration, and a gated registration must have a declared capability.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "indexing",
          "metadata",
          "parsing",
          "matching",
          "quality",
          "renaming",
          "import",
          "download",
          "notification",
          "media-kind",
          "curation",
          "network",
          "storage",
          "telemetry-sink",
          "language"
        ]
      }
    },
    "mediaKinds": {
      "description": "A transitional declaration of media kinds for legacy shape-based extensions. It requires the 'media-kind' capability when present. Typed media extensions omit it because Host derives their kinds from the admitted definition; holding the capability does not require an entry here.",
      "type": "array",
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "identifiers": {
      "description": "A transitional, non-authoritative external-identifier inventory for legacy extensions. Typed media owns identity roles, while each installed cataloger contributes the schemes and release markers it owns.",
      "type": "array",
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "tokens": {
      "description": "A transitional naming-token inventory for legacy extensions. Typed media omits it because Host derives tokens from the prepared kind. When present, names are compared by their invariant-lowercased alphanumeric grammar identity and must agree exactly with the prepared or legacy shape. A bare string is accepted and widened to a descriptor with an empty description.",
      "type": "array",
      "items": {
        "oneOf": [
          {
            "type": "string",
            "pattern": "^\\{[^{}]+\\}$"
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": ["name"],
            "properties": {
              "name": { "type": "string", "pattern": "^\\{[^{}]+\\}$" },
              "description": { "type": "string" },
              "exampleValue": { "type": "string" },
              "isRequired": { "type": "boolean" }
            }
          }
        ]
      }
    },
    "policies": {
      "description": "A transitional policy inventory for legacy extensions. Typed media omits it because policy is compiled from typed declarations. The validator currently checks the closed category shape plus nonblank, unique identifiers; it does not treat this list as executable policy authority.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "parsing": { "$ref": "#/$defs/policyList" },
        "matching": { "$ref": "#/$defs/policyList" },
        "quality": { "$ref": "#/$defs/policyList" },
        "import": { "$ref": "#/$defs/policyList" },
        "naming": { "$ref": "#/$defs/policyList" }
      }
    }
  },
  "$defs": {
    "policyList": {
      "type": "array",
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    }
  }
}
